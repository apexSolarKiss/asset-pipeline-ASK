# Multi-Output Generation / Capture Decision Note // SKU-Driven Furniture v1

## Current Decision

Current decision:

- hold
- choose Option A // Hold
- no implementation is authorized
- no Airtable mutation is authorized
- no schema creation is authorized

The repo has earned a decision note on the multi-output generation / capture question.

It has not earned implementation.

## Why The Decision Is Hold

`PKT-SKU-007` was a fresh product / fresh packet / clean first-pass run.

It required a three-view output set:

- front three-quarter hero view
- profile / side supporting view
- detail crop focused on construction or material character

Airtable produced one generated image.

The generated image corresponded most closely to the profile / side supporting view.

The hero and detail crop were missing.

That created:

- `fresh-run counter-pressure`

The structural pressure here is output-side representation and capture pressure, not merely input-side attachment pressure.

The repo has therefore earned a narrow decision note.

What it has not earned is:

- implementation
- Airtable mutation
- schema creation
- first-class output-slot adoption

One clean bounded run is enough to preserve structural pressure.

It is not enough to harden first-class output-slot architecture.

## What Hold Means

Hold means:

- preserve the fresh-run counter-pressure as repo-local truth
- make no implementation move now
- make no Airtable mutation now
- make no schema move now
- do not adopt Option E yet

This does **not** reduce the importance of the pressure.

It preserves the pressure while declining to harden structure before the evidence has earned that move.

## Option E Status

Option E // first-class output-slot table / output-slot layer remains the strongest candidate abstraction if escalation fires.

That is because it most directly answers the pressure surfaced by `PKT-SKU-007`:

- separate addressability before generation
- separate evaluability after generation
- preservation of output-set burden from prompt through capture

But the repo is not adopting Option E now.

At current evidence strength, Option E remains the leading candidate under escalation, not the current decision.

## Trigger Classification

The seven reopen / escalation triggers remain:

- another fresh multi-output packet reproduces the same single-output failure
- a multi-output field-agent run continues to return only one image despite explicit multi-view burden
- append / replacement / capture behavior prevents reliable asset identity
- downstream destination mapping requires each expected asset to be separately addressable
- governed-output evaluation cannot reliably distinguish hero / profile / detail roles in one attachment bucket
- Airtable field or attachment behavior makes output identity ambiguous across reruns
- multi-output burden cannot be preserved from prompt to generated asset capture without slot-level representation

Use the following distinction:

- evidence-type trigger = requires another bounded generation / capture run to fire
- design-type trigger = can fire from downstream prototype-buildout, destination mapping, or governed-output evaluation work without requiring another generation run

### Evidence-Type Triggers

- another fresh multi-output packet reproduces the same single-output failure
- a multi-output field-agent run continues to return only one image despite explicit multi-view burden
- Airtable field or attachment behavior makes output identity ambiguous across reruns

Evidence-type triggers likely call for another bounded run or a run-specific findings note before a structural move is made.

### Design-Type Triggers

- downstream destination mapping requires each expected asset to be separately addressable
- governed-output evaluation cannot reliably distinguish hero / profile / detail roles in one attachment bucket
- multi-output burden cannot be preserved from prompt to generated asset capture without slot-level representation

Design-type triggers may justify moving directly toward a structural decision even without another generation run.

### Mixed Evidence / Design Trigger

- append / replacement / capture behavior prevents reliable asset identity

This mixed trigger may need either route depending on how it fires.

If it appears through a later bounded run, it may first require a findings note.

If it appears through structural mapping or evaluation work, it may justify a narrower structural decision without waiting for another run.

## Reopen Posture

Absent one of the named triggers, the current hold decision remains in force.

If escalation does fire, Option E remains the leading candidate abstraction to evaluate first.

That does **not** mean any trigger automatically authorizes implementation.

It means the next artifact should be chosen according to the type of trigger that fired.

## Current Conclusion

The current conclusion is:

- current decision: hold
- current next action: no implementation
- Option E remains the leading candidate if escalation fires

If a trigger fires, the next artifact should likely be either:

- a bounded follow-up findings note, if the trigger is evidence-type
- a narrow structural decision / implementation plan, if the trigger is design-type

`PKT-SKU-003` remains a separate credit-reset diagnostic and is not required to preserve this hold decision.

Screenshots and image assets remain outside the repo.

## Out Of Scope

This note does not authorize:

- implementation
- Airtable mutation
- schema creation
- rerunning `PKT-SKU-003`
- rerunning `PKT-SKU-007`
- generalized output doctrine
- cross-mode conclusion

It also does not reopen the full options comparison.

That work is already recorded elsewhere.
