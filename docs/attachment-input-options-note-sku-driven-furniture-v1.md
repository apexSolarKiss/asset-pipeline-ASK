# Attachment-Input Options Note // SKU-Driven Furniture v1

## Purpose

This note evaluates the smallest honest decision space now earned by the bounded attachment-input comparison findings for `asset-pipeline-ASK // SKU-Driven Furniture v1`.

The question here is narrow:

- what options now honestly exist after `PKT-SKU-006` earned `spec-earned pressure` on the attachment-input question?

This is an options note only.

It does not authorize implementation, schema change, generalized attachment-input rollout, or cross-mode doctrine.

## Current Earned Pressure

Repo-local truth now supports a narrower conclusion than the earlier live-run findings or comparison plan alone.

What has been earned is:

- prompt-only and attachment-backed input did not appear behaviorally equivalent for SKU-mode product-truth fidelity in the `PKT-SKU-006` comparison
- all five pre-registered comparison criteria moved in the same positive direction
- that was enough to earn `spec-earned pressure`

What has not been earned is:

- broad runtime doctrine
- generalized attachment-input doctrine
- a schema change
- a cross-mode claim
- an implementation move inside this note

That means the repo has earned a narrow options discussion, but not a decision yet.

## Boundary

This note stays inside current repo-local discipline.

It does not propose:

- generalized attachment-input doctrine
- schema redesign
- multi-packet rollout
- cross-mode rollout
- runtime/orchestration architecture

It also does not use Run 1 collateral-generation behavior as justification for this options note.

## Evaluation Standard

Any next step should be judged against the same question:

- what is the smallest honest response to one bounded finding that attachment-backed input appeared to improve SKU-mode product-truth fidelity in one comparison?

Each option below is evaluated on:

- what it does
- what it preserves
- what it costs
- whether it stays inside current SKU-driven Furniture v1 discipline
- what would need to happen before it becomes decision-worthy

## Option A: Hold

### What It Does

Preserve the `PKT-SKU-006` finding and make no structural change yet.

Under this option:

- the finding remains recorded
- no implementation move occurs
- the question is revisited only if a named reopen trigger fires

### What It Preserves

This option preserves the strongest current boundary:

- the repo keeps the earned finding
- the repo does not over-read one bounded comparison into a structural decision
- attachment-input pressure remains interpreted as evidence, not doctrine

### What It Costs

The cost is that the repo does not learn whether the pressure reproduces.

That means the current result remains informative but narrow.

### Whether It Stays Inside Current v1 Discipline

This option stays closest to current v1 discipline.

It treats `spec-earned pressure` as enough to open a decision space, but not enough to choose inside that space yet.

## Option B: One Bounded SKU Robustness Rerun

### What It Does

Run one follow-up comparison on another SKU-driven packet or comparable SKU-mode case.

The purpose would be:

- test whether the pressure reproduces inside SKU-driven Furniture v1

Scope should remain:

- one packet
- one comparison

### What It Preserves

This option preserves the current SKU-local framing while pressure-testing it.

If the same directional improvement appears again, the repo would have stronger grounds for a later SKU-mode-local decision note.

### What It Costs

The cost is moderate:

- one more bounded execution
- one more comparison reading
- one more findings artifact

This is still lighter and less noisy than a cross-mode check.

### Whether It Stays Inside Current v1 Discipline

This option remains comfortably inside current v1 discipline.

It tests reproducibility without widening the claim beyond SKU-mode.

## Option C: Cross-Mode Bounded Check

### What It Does

Run one comparison in another workflow mode, such as collection / merchandising or campaign / editorial.

The purpose would be:

- test whether visual anchoring pressure is SKU-specific or broader

### What It Preserves

This option preserves the possibility that the pressure may not be SKU-only.

It is the first option that could begin to challenge the current SKU-local framing if the same directional pattern appeared clearly elsewhere.

### What It Costs

The cost is higher than Option B:

- broader scope
- higher interpretation cost
- noisier comparison surface

This option is also more likely to introduce ambiguity about whether the observed pressure belongs to attachment-input structure or to mode-specific burden differences.

### Whether It Stays Inside Current v1 Discipline

This option is still possible, but it fits less cleanly than Option B.

It begins to widen the question before SKU-mode-local reproducibility has been checked.

## Option D: Small Structural Adjustment

### What It Does

Treat `workflow_packets.product_image_lookup` as a first-class SKU-mode packet-input convention.

Under this option:

- the convention would be documented as SKU-mode-local only
- it would not be generalized across modes
- it would not be treated as broad runtime doctrine

### What It Preserves

This option preserves the possibility of a small local structural response without overclaiming a platform rule.

It is the lightest structural move available if the repo later decides that the `PKT-SKU-006` pressure is strong enough to justify a SKU-mode-local convention.

### What It Costs

The cost is real:

- the repo would be making a structural choice from still-narrow evidence
- even a local convention risks sounding more settled than the evidence currently supports

### Whether It Stays Inside Current v1 Discipline

This option can stay inside current v1 discipline only if it is kept explicitly SKU-mode-local and clearly bounded.

At current evidence strength, it still looks more like a candidate later decision than the smallest honest immediate move.

## Narrow Comparison

Across current repo-local truth, the options sort as follows:

- Option A is the strongest boundary-preserving move
- Option B is the narrowest additional evidence move
- Option C is broader, higher-cost, and noisier than Option B
- Option D is the lightest possible structural adjustment, but it still requires more justification than a pure hold or one more bounded SKU comparison

That does not make the choice automatic.

It does mean the burden of proof rises as the repo moves from hold, to one more bounded SKU check, to cross-mode testing, to structural adjustment.

## Reopen / Escalation Triggers

This question should move from options-note posture toward a later decision note or structural change only if at least one of the following occurs:

- a second independent SKU-mode comparison reproduces the same directional improvement
- prompt-only generation demonstrably fails product-truth sufficiency while attachment-backed generation restores governability
- an attachment-backed input path becomes necessary to preserve candidate-output attribution or product-truth fidelity in a later bounded run
- a non-SKU mode shows the same pressure clearly enough to challenge SKU-only framing

Absent one of those triggers, the repo should remain inside the current options space rather than forcing a decision.

## What Keeps The Repo At Options-Only Posture

The repo should remain at options-only posture if:

- current evidence still comes from one bounded comparison packet
- the current finding remains SKU-mode-local rather than cross-mode
- no second independent reproduction has yet occurred
- any structural move would still depend more on preference than on repeated pressure

Under current repo-local truth, this is still the correct posture.

## Explicitly Out Of Scope

This note does not authorize:

- generalized attachment-input doctrine
- schema redesign
- multi-packet rollout
- cross-mode rollout
- runtime/orchestration architecture
- reopening prompt-artifact continuity unless its own triggers fire independently

It also does not treat Run 1 collateral-generation behavior as part of the attachment-input pressure analysis.

## Current Lean

The smallest honest current lean is:

- Option A deserves first consideration
- Option B is the narrowest next evidence move if the repo decides more evidence is needed now

That lean is narrow, not decisive.

It does not choose among the options.
It records only the smallest honest decision space now earned by the `PKT-SKU-006` finding.
