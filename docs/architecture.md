# Architecture

The goal of this repository is to define a structured information architecture for scalable visual asset production systems, with particular emphasis on how style guides, creative rules, and other governing constraints can become machine-usable normative structure for production workflows that include AI generation.

At this stage, the correct architecture is the smallest honest architecture: enough structure to support disciplined design, but not so much structure that it hardens assumptions that have not yet earned permanence.

The architecture is intentionally high-level and leaves major design questions open, including:

- workflow taxonomy
- ontology for intent, inputs, constraints, orchestration, outputs, and governance
- where authorship-bearing discretion enters and where governance-bearing curation resolves
- where constraints bound that discretion
- where approvals and override points live
- whether the brand-system layer eventually carries first-class structural representation, terminal operator-mediated translation, or a hybrid
- how the system extends from still image to video
- how deterministic layers relate to agent-assisted layers

The campaign-mode fork-resolver test (`PKT-CAMP-004`; Phase D bounded `refines` reading) and the visual identity system deepening have pressured several of these open questions at bounded depth; the architecture has absorbed the pressure into the current shape below but has not closed any of them.

The repository is expected to support still-image workflows first. Video should be treated as an extension path, not as a reason to prematurely over-generalize the initial architecture.

## Where This File Sits

This file is the **current-architecture front door**: a concise statement of where the architecture currently sits, what it has earned, and where the substantive content lives. It is intentionally compact and routes to depth rather than reproducing detail.

Source-of-truth relationships across the load-bearing artifacts:

- **`docs/architecture.md` (this file)** — current front-door synthesis; the architecture's current shape, evidence depth, and held questions at orientation depth
- **`docs/milestones/milestone-11-zone-6-option-f-carrier-shape-plateau.md`** — the current plateau marker; absorbs the Zone 6 / Option F trace carrier shape arc across PR #250–#261 (failure mode → conceptual posture → empirical support across no-contradiction / single-contradiction / multi-contradiction substrates → bounded constraint vocabulary → carrier-shape choice sharpened-not-closed)
- **`docs/milestones/milestone-10-phase-2-six-category-synthesis-plateau.md`** — the prior plateau marker; absorbs the Phase 2 six-category brand-system input completion
- **`docs/milestones/milestone-9-curation-semantic-split-brand-system-decision-surface-plateau.md`** — milestone-9 plateau marker
- **`docs/milestones/milestone-8-phase-2-structured-model-plateau.md`** — milestone-8 plateau marker
- **`docs/structured-ia-model-v2.md`** — the current canonical machine-readable structured enumeration of the IA layered-content model (hybrid prose + YAML appendix); absorbs the stable enumeration earned through milestone 10 (17 top-level YAML keys including the six-kind force vocabulary, the nine-fold reference-function taxonomy, the nine-site discretion decomposition, the four-kind discretion classification, the seven-stage extraction sequence with default-graph behavior, the four-bucket category function classification with per-category assignments, the nine-relation cross-category vocabulary, and the five trace dimensions plus the temporal-state candidate); held content (carrier shapes, the five-or-six-zone hybrid implication, cross-category relation pair instances, extraction-sequence graph encoding, sub-classifications of derivation-basis) remains prose
- **`docs/structured-ia-model-v1.md`** — predecessor structured form; v1's nine top-level YAML keys are preserved (with `deepened_input_categories` and `held_questions` updated at v2); v1 remains authoritative for v1 content depth but is not the current canonical structured model
- **`docs/architecture-apex-definition-layer-v1.md`** — the v1 apex definition-layer artifact at SKU + same-category-collection scope; depth-authority for synthesis-pass content and the three Named Limitations carried forward
- **`docs/campaign-mode-fork-resolver-test-phase-d-findings-v1.md`** — Phase D bounded `refines` reading at PKT-CAMP-004 scope; depth-authority for the curation-premise fork's current status and the curation semantic split
- **`docs/normative-force-and-durable-layer-pressure-v1.md`** — post-milestone-9 normative-force pass v1; depth-authority for the six-kind force-kind criterion set at current evidence depth
- **`docs/brand-system-carrier-decision-surface-v2.md`** — current brand-system carrier decision surface; depth-authority for the zone-by-zone narrowing of the brand-system carrier decision (hybrid is operative for Zones 1 and 2 at the brand-system layer; Zone 1A approved references structurally earned; Zone 1B brand-wide constants beyond approved references is the residual held question; Zone 2 is deliberate prose / operator-mediated; Zones 3–6 are rerouted to adjacent architectural locations — packet/slot, governed-asset / reference-linkage, reference-asset lifecycle, intake / governance — and are not brand-system-layer carrier questions)
- **`docs/brand-system-hybrid-carrier-decision-surface-v1.md`** — predecessor brand-system carrier decision surface (post-milestone-9; four-of-six deepening depth); depth-authority for the original three-shape comparison and the three-zone hybrid implication; preserved as historical record
- **`docs/brand-intake-judgment-trace-dry-run-v1.md`** — post-milestone-9 brand intake judgment-trace dry run v1; depth-authority for the sparse-articulation walk + Outcome read + the Scale-of-Operation Held Question that resulted
- **`docs/continuity-breaking-intake-trace-test-v1.md`** — Zone 6 continuity-breaking intake trace test v1 (first operational pressure surface post-milestone-10); depth-authority for the simulated continuity-break finding (aggregate result: trace partially breaks — most trace dimensions fail across most content pieces; enum-shaped content tied to mode-anchored vocabulary and structurally-explicit linkage preserve partial success); held trace candidates moved from paper-pressed to operationally-pressed; no carrier shape earned; sub-classification surfaced (five-value `derivation_basis` candidate: lifted-verbatim / inferred-from-evidence / worked-example-default / operator-authored / unresolved-marked); negative-space trace surfaced as sub-dimension of presence-of-inspection
- **`docs/intake-trace-carrier-decision-surface-v1.md`** — intake trace carrier decision surface v1 (converts PR #250's reconstruction-failure points into a carrier-shape comparison); depth-authority for the six-option carrier-shape comparison (A prose-only / B inline annotations / C per-clause tags / D per-source-layer inspection log / E intake decision record / F hybrid D+E) and the Option F surviving conceptual posture (hybrid inspection log + intake decision record addresses all six PR #250 trace dimensions); minimum conceptual trace vocabulary named without schema (presence_of_inspection; articulation_state; fallback_chain_position; derivation_basis with five-value; contradiction_resolution; negative_space_trace; source_layer; resolved_content_piece; target_ia_layer); temporal_state intentionally Zone 5 (adjacent decision surface), not Zone 6; explicit non-promotion of any carrier shape to schema
- **`docs/real-multi-operator-continuity-break-test-v1.md`** — real multi-operator continuity-break test v1 findings (Stage C of the Zone 6 trace test sequence; scores a fresh reader's reconstruction against the dry-run-derived ground-truth key for mocked Option-F carriers); depth-authority for the operational finding (aggregate 10/10 SUCCESS; trace holds at this test); shifts PR #250's simulated "trace partially breaks" against no carriers to "trace holds" against mocked Option F carriers; Option F operationally supported at one-reader / one-substrate / mocked-shape depth; explicit non-earning of specific carrier shape / schema / structured IA v3 / validator / orchestration; contradiction-resolution dimension non-pressured (substrate has no contradictions); multi-reader / cross-substrate / long-context / temporal-pressure generalization unearned; granularity (content-piece-level vs clause-level) and reference-opacity (asset_metadata empty for reference governance) newly surfaced as shape-design concerns
- **`docs/option-f-trace-carrier-shape-design-surface-v1.md`** — Option F trace carrier shape design surface v1; depth-authority for the bounded carrier-shape constraint vocabulary the PR #254 empirical support enables (required by PR #254: per-source-layer inspection log; intake decision record; five-value `derivation_basis`; shared intake-event identifier; negative-space trace via inspection-log value space) (structurally suggested: bidirectional inspection-log ↔ decision-record linkage; content-piece-level granularity as floor with mixed-granularity pattern; reference-selection rationale carrier among three structural alternatives; rejected-alternatives carrier as complement) (held pending contradiction-present substrate: contradiction-resolution carrier shape) (held pending cross-substrate pressure: clause-level granularity; multi-source divergence behavior; long-context durability; value-space extension for articulation-state and derivation-basis) (rejected: prose-only reference rationale; event-level granularity; field-level granularity; collapsing the five-value `derivation_basis`); explicit non-authoring of schema, entity placement, field types, cross-entity relationships, rollup patterns, structured IA v3, validator, or orchestration; Zone 6 vs Zone 4 boundary preserved (reuse-time reference governance routed to a held Zone 4 decision surface)
- **`docs/option-f-contradiction-present-continuity-break-test-v1.md`** — Option F contradiction-present continuity-break test v1 findings (scores a fresh reader's reconstruction against the dry-run-derived-plus-contradiction-overlay ground-truth key for mocked Option-F carriers with a populated contradiction_resolution sub-field on the carried decision record); depth-authority for the operational finding (aggregate 13/13 SUCCESS; trace holds at this test; contradiction-resolution sub-aggregate 4/4 — all eight sub-dimensions including presence-of-contradiction, conflict-source identification, rejected-source content, carried-source content, carried-source identification, resolution rule, architectural basis, and cross-cutting summary reconstructed); addresses the contradiction-resolution dimension PR #256 held pending a contradiction-present substrate; Option F's contradiction-resolution conceptual posture operationally supported at one-reader / one-substrate / one-contradiction / clean-resolution-rule / mocked-shape depth; the new `articulation_state: contradictory` value operationally supported as architecturally distinct from `absent`; base trace dimensions held under contradiction-overlay pressure (no regression from PR #254 baseline); explicit non-earning of specific contradiction-resolution carrier shape (the mocked sub-field-on-decision-record shape worked but the test does not differentiate it from the other two PR #256-named structurally-suggested alternatives — separate `contradiction_log` entity; annotation on inspection log entry); multi-reader / multi-contradiction / ambiguous-resolution-rule / cross-substrate / long-context generalization unearned; clause-level granularity remains held per reader-flagged weakness across both PR #254 and this test
- **`docs/option-f-multi-contradiction-continuity-break-test-v1.md`** — Option F multi-contradiction continuity-break test v1 findings (extends PR #258's single-contradiction baseline to a two-event substrate with mixed 1:1 and 1:many event structures); depth-authority for the operational finding (aggregate 14/14 SUCCESS; trace holds at this test; multi-contradiction sub-aggregate 5/5 — Event 1 1:1 reconstructed, Event 2 multi-source convergent carriage + partial-architectural-grounding + 1:many decision impact reconstructed, cross-event structural patterns including duplication-of-Event-2-across-D1-and-D2 reconstructed); Option F's multi-contradiction conceptual posture operationally supported at one-reader / one-substrate / two-contradiction / mixed-1:1-and-1:many / clean-and-partial-architectural-grounding / mocked-shape depth; the new `articulation_state: partial-with-internal-conflict` value operationally supported as architecturally distinct from `contradictory`, `partial`, and `absent`; multi-source `carried_source` list representation, partial-architectural-grounding distinction, and `affected_decisions` cross-reference all reconstructable; base trace dimensions held under multi-contradiction-overlay pressure (no regression from PR #254 + PR #258 baselines); explicit non-earning of specific contradiction-resolution carrier shape — the test does NOT close PR #256's three-alternative choice but sharpens it: the extended sub-field-on-intake-decision-record shape works operationally, AND the reader explicitly identified Event 2's duplication across D1/D2 as a structural pressure point, which strengthens the case that a separate `contradiction_log` entity remains materially plausible (it would naturally dedupe what the sub-field shape duplicates) and that annotation-on-inspection-log remains materially plausible for source-internal-inconsistency cases; multi-reader / three-or-more-contradiction / ambiguous-resolution-rule / cross-substrate / long-context generalization unearned; clause-level granularity remains held per reader-flagged weakness consistent across PR #254 + PR #258 + this test
- **`docs/intake-reference-rationale-carrier-decision-surface-v1.md`** — intake-reference-rationale carrier decision surface v1 (first post-milestone-11 architectural move; design-surface successor to PR #256's pattern but for a distinct architectural seam — intake-time reference rationale rather than contradiction-resolution); depth-authority for the bounded carrier-shape constraint vocabulary for intake-time reference-rationale; addresses the reference-rationale gap that three independent reader-flag signals across the Zone 6 / Option F arc (PR #254 + PR #258 + PR #260) named as architecturally material (selected references are structurally carried but selection rationale, per-reference contribution, evidence basis, and rejected alternatives are not); carrier-shape options labeled: R-A prose-only rationale REJECTED (the empirical failure mode three readers flagged); R-B `asset_metadata` extension STRUCTURALLY SUGGESTED for per-reference intrinsic rationale (function from PR #239 nine-fold taxonomy; contribution-categories that travel with the reference across uses); R-C separate reference-selection-rationale record STRUCTURALLY SUGGESTED for event-level rationale with rejected alternatives naturally accommodated; R-D attachment to intake decision record STRUCTURALLY SUGGESTED for event-level rationale tied to decision context (parallel to PR #256's mocked sub-field shape); R-E rejected-alternatives complement STRUCTURALLY SUGGESTED AS COMPLEMENT; hybrid posture (R-B + R-C OR R-D + R-E) STRUCTURALLY SUGGESTED as conceptual posture with R-C vs R-D HELD pending operational pressure (analogous to PR #256's stance on contradiction-resolution carrier-shape choice); required by reader-flag evidence (carrier-attached not prose-only; per-reference contribution reconstructable; rejected alternatives reconstructable; reference function reconstructable; evidence basis reconstructable); held pending operational pressure (R-C vs R-D selection; selection-rationale category vocabulary; articulation-state at per-reference level; confidence vocabulary; multi-reference event scaling); held pending cross-substrate pressure (negative/avoid reference handling; reference-rationale durability across reuse contexts); explicit non-authoring of schema, entity placement, field types, cross-entity relationships, rollup patterns, structured IA v3, validator, or orchestration; Zone 6 vs Zone 4 boundary preserved (reuse-time governed-output continuity / derivative governance is Zone 4 — held as adjacent decision surface, not collapsed into intake-time rationale scope)
- **`docs/intake-reference-rationale-operational-pressure-test-v1.md`** — intake-reference-rationale operational pressure test v1 findings (Stage C of the operational pressure test PR #263's Self-Superseding Clause named; mocks R-D shape — sub-field on intake decision record D3 — within an R-B + R-D + R-E hybrid posture; pressures fresh-reader reconstruction of selected references, per-reference contribution, set-level rationale, and rejected alternatives); depth-authority for the operational finding (aggregate 11/11 SUCCESS; trace holds at this test; reference-rationale sub-aggregate 7/7; rejected-alternatives sub-aggregate 3/3); all scored dimensions reconstructed without partial or failure marks including the within-packet multi-layer attribution case (REF-KT-003), the multi-function-but-single-layer case (REF-KT-004), and the two-rules-applied rejection discrimination (visual-as-fidelity-strategy for REJ-A + REJ-B; SKU-driven mode hero-pattern boundary for REJ-C); R-B + R-D + R-E mocked hybrid held at one-reader / one-substrate depth; R-D was sufficient at this substrate depth AND structurally near the edge per the reader's Q-11 carrier-shape pressure observation ("the pressure signal is not information loss; it is object-boundary ambiguity"); R-C remains materially plausible under growth conditions (more references; more rejection classes; conflicting evidence; partial selection rationales) per the reader's directional signal; Q-11 is unscored diagnostic signal, not proof; the test does NOT close the R-C vs R-D choice in either direction — only R-D was mocked, R-C judgment is reader extrapolation from R-D experience; multi-reader / cross-substrate / symmetric-R-C-mocked / growth-pressure / multi-event / combined-with-Option-F-carriers / ambiguous-rejection-rule / long-context generalization remain held; explicit non-earning of specific carrier shape / schema / structured IA v3 / validator / orchestration; Zone 6 vs Zone 4 boundary preserved (Zone 4 reuse-time governed-output continuity remains adjacent and held)
- **`docs/governed-output-continuity-reuse-derivative-governance-decision-surface-v1.md`** — Zone 4 governed-output continuity / reuse-derivative governance decision surface v1 (first focused architectural treatment of Zone 4 since PR #248's brand-system carrier decision surface v2 located it; parallels PR #263's design-surface pattern for intake-time but addresses a structurally distinct architectural object — governed outputs in their reuse role); depth-authority for the Zone 4 carrier-shape constraint vocabulary; addresses the dual-nature governed-output question (the same asset may exist as terminal output at the moment of governance AND as reference asset at later moments); carrier-shape options labeled: Z4-A prose-only reuse notes REJECTED (same failure mode reader-flagged across PR #254 / #258 / #260, architecturally extends to reuse); Z4-B governed-output metadata extension STRUCTURALLY SUGGESTED for asset-intrinsic state (continuity-anchor flag; family memberships; supersession state; derivative-children list); Z4-C separate reuse / derivative event record STRUCTURALLY SUGGESTED for event-level reuse trace; Z4-D reference-linkage annotation STRUCTURALLY SUGGESTED for per-linkage reuse-time annotation; Z4-E asset-family / lineage record STRUCTURALLY SUGGESTED for family-coherence scope; hybrid posture (Z4-B + Z4-C OR Z4-D + Z4-E + reuse-rejected complement) STRUCTURALLY SUGGESTED as conceptual posture with Z4-C vs Z4-D HELD pending operational pressure (parallel to PR #263's R-C vs R-D stance and PR #256's three-alternatives stance for contradiction-resolution); required by architectural premise (carrier-attached not prose-only; reuse-type distinction reconstructable; source-authority-type reconstructable; downstream inheritance limits reconstructable; family-coherence relationships reconstructable; supersession events traceable); held pending operational pressure (Z4-C vs Z4-D selection; reuse-type vocabulary; authority-type vocabulary; inheritance-dimension vocabulary; per-event approval-recurrence vocabulary; family-rule vocabulary); held pending cross-substrate pressure (multi-mode reuse patterns; cross-brand reuse; cross-time reuse; cross-category reuse); explicit non-authoring of schema, entity placement, field types, cross-entity relationships, rollup patterns, structured IA v3, validator, or orchestration; boundary preserved (intake-time reference rationale — PR #263 axis — is named adjacent and not collapsed into Zone 4 reuse-time scope; the two surfaces address structurally different architectural objects)
- **`docs/layered-reference-and-discretion-architecture-v1.md`** — post-milestone-9 layered reference and discretion architecture v1; depth-authority for the nine-fold reference-function taxonomy, the normative hierarchy framing across the eight IA layers, and the nine-site creative-discretion decomposition with per-site trace-gap analysis
- **`docs/brand-discovery-digestion-layered-intake-architecture-v1.md`** — Phase 2 completion arc; depth-authority for the layered intake architecture, the seven-stage extraction sequence (`evidence → articulated conventions → abstraction → application → validation → defaults → inference/authorship`), the operator-authored vs inferred-not-stated distinction, and the three-zone hybrid implication for the brand-system layer
- **`docs/brand-system-input-application-guidelines-to-ia-mapping-v1.md`** — Phase 2 fifth deepening; depth-authority for application guidelines as the operationalization layer of the brand-system stack
- **`docs/brand-system-input-past-campaigns-to-ia-mapping-v1.md`** — Phase 2 sixth and final deepening; depth-authority for past campaigns as historical execution evidence (not brand doctrine by default) and the temporal-state trace candidate dimension
- **`docs/brand-system-input-cross-category-synthesis-v1.md`** — Phase 2 cross-category synthesis; depth-authority for the six-category comparison matrix, the nine-relation cross-category vocabulary, the extraction-sequence refinement (default graph with conditional jump-points and back-feedback edges), and the five-or-six-zone hybrid implication for the brand-system layer
- **`docs/index.md`** — full navigation across all repo artifacts; this file does not replicate that navigation
- **Source artifacts (Phase 1 sketches, Phase 2 deepenings, operational findings, cross-mode probes)** — depth-authority for content; this file does not duplicate them

A returning reader landing here gets the architecture's current state and routes to depth. A reader needing per-mode IA content reads the Phase 1 mode sketches; per-input-category mapping reads the Phase 2 deepenings; per-packet operational findings reads the corresponding finding artifact; full repo navigation reads `docs/index.md`.

### Scope of current claims

This file scopes its own current-state claims to evidence depth, distinct from the project's target layer.

- **Target layer**: machine-usable normative structure / durable spec and orchestration layer above changing tools and models. This is the project's intent (see `README.md`).
- **Current state**: structured IA model v2 (hybrid prose + YAML appendix; v1 is the predecessor structured form) plus drift detection equals a machine-*readable* enumeration with documentation-drift coverage. v2's appendix grew from v1's nine top-level keys to seventeen, absorbing the post-milestone-9 normative-force vocabulary (descriptive / directive / constraint / reference / judgment / governance, pressured against PKT-SKU-009, PKT-COLL-001, PKT-CAMP-004), the PR #239 reference-function taxonomy and discretion decomposition, the PR #241 seven-stage extraction sequence with default-graph behavior descriptor, the PR #244 four-bucket category function classification and nine-relation cross-category vocabulary, and the five trace dimensions plus the temporal-state candidate. It is still not a machine-*usable* orchestration spec — no code path or workflow currently consumes the YAML appendix or the force-kind vocabulary as orchestration input.
- **Conceptual claims newly earned post-milestone-9 (PR #239)**: (a) the layered IA is a normative hierarchy — each layer carries normative authority over downstream layers; (b) references must be typed by function before they can be modeled by carrier; (c) creative discretion must be decomposed by site before curation / governance semantics can be generalized; (d) category-layer inheritance is partial — some content (e.g., lighting register) plausibly inherits cross-category under shared brand-system aesthetic, while environment composition and product-class proportions are category-local; (e) scale beyond operator continuity is treated as an architectural premise of the project, with scale legibility as a test criterion at each layer and site. Depth-authority: `docs/layered-reference-and-discretion-architecture-v1.md`.
- **Conceptual claims newly earned at the Phase 2 six-category synthesis plateau (PR #241 + #244)**: (a) brand discovery / digestion is modeled as a layered intake architecture — production-side on-ramp from a brand-system stack into the layered IA — with a seven-stage extraction sequence that behaves as a default graph with conditional jump-points and back-feedback edges, not a strict linear order; (b) operator-authored content is architecturally distinct from inferred-not-stated content (the former lacks an evidence basis; the latter is derived from evidence under sparsity); (c) the six brand-system input categories carry six distinct functional roles classified across four buckets (evidence / articulation / application / validation), with asset library and past campaigns the two evidence-shaped categories and the other four mediating between brand platform's abstraction pole and past campaigns' execution pole; (d) the nine-relation cross-category vocabulary (validates / contradicts / supersedes / operationalizes / demonstrates / abstracts / constrains / inherits / defers / fails-to-answer) is articulable across the six-category set; (e) the brand-system structural decision's hybrid implication is sharpened from three zones (PR #241) to five or six zones (PR #244) — stable brand-wide constants + approved references; prose / operator-mediated translation; per-touchpoint and per-mode constraint structural representation; governed-output continuity reference elevation; temporal-state trace dimension; intake-time judgment trace zone. Depth-authorities: `docs/brand-discovery-digestion-layered-intake-architecture-v1.md`; `docs/brand-system-input-cross-category-synthesis-v1.md`.

The architecture's current-state claims below are scoped accordingly.

## Current Architecture Shape

The architecture's current shape, abbreviated:

**Layers.** Eight IA layers carry across the four worked-example modes. The layered IA is a normative hierarchy: each layer carries normative authority over downstream layers, and the inheritance / override / combination / conflict behavior between layers is the normative-hierarchy mechanism the architecture implements (depth-authority: `docs/layered-reference-and-discretion-architecture-v1.md`).

- brand-system layer (currently unmodeled in IA; the brand platform and visual identity system deepenings concretize this as a real architectural gap; the brand-system hybrid carrier decision surface v1 tests the three live shapes against the Phase 1 criterion set without resolving the decision — see Brand-System Layer Status below)
- category / product-class layer (signaled-only; not first-class; inheritance from brand-system is partial — lighting register plausibly inherits cross-category under shared brand-system aesthetic, while environment composition and product-class proportions are category-local)
- mode-specific layer (varies by mode; see below)
- packet layer (strongest current carrier)
- slot layer (role-specific narrowing)
- candidate generation layer (transient attachments)
- curation seam — two distinct senses observed:
  - **authorship-bearing discretion** (where creative judgment actually happens; site varies by mode and packet; appears concentrated at the seam in SKU-mode evidence; surfaced upstream at prompt-authoring and prompt-remediation in PKT-CAMP-004 campaign-mode evidence)
  - **governance-bearing curation** (where output becomes governed asset; always at the seam; carries five-axis provenance + curator + `curation_pattern`)
  - These may co-locate (SKU evidence appears to) or separate (campaign at PKT-CAMP-004 did). The architecture had been collapsing the two senses. The normative-force pass v1 articulates this further as judgment-force-kind at multiple sites of authorship-bearing form, with governance-force-kind always at the seam.
- governed asset layer (durable terminal output; carries `asset_attachment` self-contained writeback)

**Modes.** Four worked-example modes, each with a mode-specific layer between category and packet (SKU-driven mode has none; brand campaign / editorial mode has two):

- SKU-driven product imagery — no mode-specific layer; SKU is the anchor
- collection / merchandising — collection / grouping layer
- marketing / message-driven — message / offer / communication layer
- brand campaign / editorial — campaign concept layer + review-context / campaign-world coherence layer

**Carriers.** Five carrier types observed across the architecture: prose carriers; structural carriers; visual reference carriers; governance carriers; held candidates (paper-pressed but not earned). Carrier distribution varies by content category — prose-heavy for some inputs, structurally-explicit-heavy for others, almost entirely prose-carried with structural void for non-visual brand-system content.

**Normative-force vocabulary.** Six candidate kinds of normative force articulate what kind of authority each piece of information carries across the architecture: **descriptive** (what is true); **directive** (what should happen); **constraint** (what must not be violated); **reference** (what visually anchors interpretation); **judgment** (what was resolved by a human, operator, or agent); **governance** (what elevates output into governed asset identity). At current evidence depth across PKT-SKU-009, PKT-COLL-001, and PKT-CAMP-004: descriptive (product scope), reference, governance, and seam-level judgment-trace carry cleanly via structural carriers; packet-level directive and constraint share carrier (multilineText) without structural distinction; intake-time judgment trace has no current carrier (see Scale-of-Operation Held Question below). The vocabulary is depth-authority in `docs/normative-force-and-durable-layer-pressure-v1.md`.

**Implementation entry surface.** Brand discovery / digestion architecture is the surface where brand-system input enters the layered IA before any tool implementation begins. All six brand-system input categories named in the Phase 2 opening sketch are now at deepening depth (photography / image style guide; asset library; brand platform; visual identity system; application guidelines; past campaigns). Brand discovery / digestion has been modeled as a layered intake architecture (PR #241) and synthesized across all six categories (PR #244). Depth-authority for the integrated intake model: `docs/brand-discovery-digestion-layered-intake-architecture-v1.md` + `docs/brand-system-input-cross-category-synthesis-v1.md`.

**Fallback chain.** When brand-system input is sparse, contradictory, or absent, the architecture falls back through an ordered six-layer chain: asset library → photography style guide → brand platform → intra-category recency / specificity → Phase 1 worked-example defaults → operator-marked inferred-not-stated. Operator judgment becomes load-bearing under stress; the architecture surfaces options at each conflict point but does not auto-resolve. The brand intake judgment-trace dry run v1 pressured this chain operationally under sparse articulation on one synthetic stack — see Scale-of-Operation Held Question below.

**Structured form.** The structured IA model v2 carries the stable enumeration above as a machine-readable YAML appendix alongside prose for unresolved content. The YAML is bounded: held questions are encoded as markers, not as structural representations of question content; held schema candidates remain in prose, not encoded. v2 supersedes v1 at the structured-form layer; v1 is preserved as the predecessor structured form and remains authoritative for v1 content depth.

The structured IA model v2 is the canonical form for stable enumeration. Anything not in its YAML appendix is either held in prose, earned only at synthesis-pass depth in the apex artifact and the Phase 1 / Phase 2 sketches, or carried in the source artifacts the appendix's new keys reference (PR #239 layered reference and discretion architecture; PR #241 layered intake architecture; PR #244 cross-category synthesis; the normative-force pass v1; the brand intake judgment-trace dry run v1).

## Evidence Depth

What's been earned at the current plateau:

**Operationally grounded (full-flow generation → curation → governance).** SKU-driven mode at PKT-SKU-007 / 009 / 010 (five-axis provenance + curator + `curation_pattern` + `asset_attachment` carry cleanly); collection / merchandising mode at PKT-COLL-001 (paper-strain composite-anchor mutation operationally deflated; prose discipline carried 1:N constituent enumeration + exclusion-by-name + cross-axis curation tradeoffs cleanly); brand campaign / editorial mode at PKT-CAMP-004-KITCHEN_PREP at bounded depth (one packet; three slots; three `generated_assets` rows; all three curation events `confirmation-bearing`; upstream authorship-bearing work at LIFE_IN_USE prompt-authoring and OBJECT_VIGNETTE prompt-remediation; Phase D recommendation `refines` as bounded read, held for further pressure-testing).

**Structurally proven; not full-flow operationally pressured.** Marketing / message-driven mode at PKT-MSG-001 (cycle 1, `findings-only`); brand campaign / editorial mode also at PKT-CAMP-001 (cycle 1 rerun; review-board context functioned as load-bearing for the bounded mini-family proof) plus the Phase 1 Campaign-Driven D2C Home-Goods v1 base setup (95-field schema mirror in a separate base). Campaign-mode's full-flow elevation comes from PKT-CAMP-004 above; cross-base + cross-brand evidence comes from the D2C base + Warm Restraint aesthetic instance.

**Paper-pressed; not yet operationally tested.** Cross-mode composite-anchor candidates (collection deflated; marketing held; campaign held + sharper); structured slot-message-element fields; selection-axis enum classification; reference-type tag (with two divergent category proposals from the visual pair); brand-platform first-class entity; brand-wide constants first-class entity (VIS-pressured); approval-state metadata; rejected-assets entity; articulation-state flag; derivation-basis field; fallback-chain-position annotation; worked-example-defaults carrier; contradiction-log entity; aspect-ratio-as-attribute; per-mode role-typical aesthetic constraints. None earned by operational pressure.

**Schema is mode-independent at structural level.** The same packet-and-seam schema carried SKU-driven, collection / merchandising, marketing / message-driven, and brand campaign / editorial workloads at first-pass depth, with further-flow operational depth varying per the rows above.

The visual-as-fidelity-strategy claim from the apex aesthetic-layering pass holds operationally (SKU + collection) and is reciprocally established on paper by the Phase 2 visual-input pair (photography style guide articulates conventions; asset library demonstrates them; when they diverge, the library carries); the VIS deepening adds that brand-wide visual *constants* (logo, typography, color, grid) carry articulated structure of their own that the IA has no first-class carrier for.

**Post-milestone-9 absorption.** Three artifacts landed as a coherent post-milestone-9 pressure sequence and are now reflected in the front-door framing above: the normative-force pass v1 supplied the six-kind force-kind vocabulary; the brand-system hybrid carrier decision surface v1 tested the three milestone-9 live shapes for the brand-system layer against the criterion set without resolving the decision (no single shape carries the criterion set cleanly; if hybrid is the surviving shape, the surface implies three zones — structural carriers for stable constants + approved references; operator-mediated translation for prose-rich directive content; an unresolved third zone of intake-time judgment trace); the brand intake judgment-trace dry run v1 pressured the third zone via a sparse-articulation walk and produced an Outcome read that current carriers carry under one-operator continuous-context scale while three intake-trace held candidates plus a fourth surfaced dimension (presence-of-inspection trace) remain on a conditional pressure path bound to a scale condition untested. The architectural question the sequence surfaced is named below as the Scale-of-Operation Held Question.

**Phase 2 six-category completion arc (milestone-10 plateau).** Four further artifacts landed as a coherent Phase 2 completion arc and are now reflected in the front-door framing above: PR #241 modeled brand discovery / digestion as a layered intake architecture with a seven-stage extraction sequence (evidence-first under the architecture's structural-carrier distribution; refined into a default graph with conditional jump-points and back-feedback edges via PR #244) and articulated operator-authored content as architecturally distinct from inferred-not-stated content; PR #242 deepened application guidelines (operationalization-layer content; per-touchpoint constraint structural representation paper-pressed); PR #243 deepened past campaigns (historical execution evidence, not brand doctrine by default; surfaces temporal-state trace as a candidate sixth trace dimension); PR #244 synthesized across all six categories (six-category comparison matrix; nine-relation cross-category vocabulary; sharpened the brand-system hybrid implication from three zones to five or six zones; named seven candidate next-pressure surfaces as implications only without authorizing any). Milestone 10 marks this plateau.

## Held Questions

Carried forward unresolved at this plateau. The architecture does not pretend to have settled them. Two are treated below as named structural concerns; the remaining held questions are inventoried.

### Curation-Premise Architectural Fork

The foundational premise that curation is the singular runtime creative-discretionary act and generation is mechanical was load-bearing across the architecture as previously stated. The PKT-CAMP-004 fork-resolver test has bounded-pressured the premise:

- **Phase D §3.1** falsified the `holds` reading at PKT-CAMP-004 scope: all three curation events were `confirmation-bearing`, and authorship-bearing work surfaced upstream at prompt-authoring (LIFE_IN_USE) and prompt-remediation (OBJECT_VIGNETTE). This falsifies the *unqualified* architecture-wide claim that curation always carries runtime authorship.
- **Phase D §4 + §7** recommended `refines` as the current best bounded read at PKT-CAMP-004 scope: upstream authorship-bearing moments shape the candidate set, and curation ratifies or rejects their output.
- **Phase D §9** preserved `fragments` as a named falsification path, not doctrine: a packet where upstream authored moments occur but do not flow into the slot's curation event would weaken `refines` in favor of `fragments`.

The substantive vocabulary refinement the bounded reading forces is the curation semantic split named in the Layers list above: authorship-bearing discretion (variable site) is distinct from governance-bearing curation (always at the curation seam).

**Mode-weighted current standing**:

- SKU-driven evidence (PKT-SKU-007 / 009 / 010 full-flow) continues to support curation as a strong authorship-bearing governance seam — authorship and governance appear to co-locate in SKU mode.
- Collection-mode evidence (PKT-COLL-001) does not contradict the SKU pattern at the depth tested, but it did not pressure the semantic split as directly as PKT-CAMP-004.
- Campaign-mode evidence (PKT-CAMP-004) showed the two senses separating: authorship upstream, governance at the seam.
- Marketing-mode and broader cross-brand / cross-category operational evidence remain unearned.

The architecture-wide framing is provisional. The singular-curation framing remains useful in SKU + collection scope where the two senses appear to co-locate; it is mode-weighted, not globally dispositive. The fork is not closed; the bounded `refines` read is held for further pressure-testing per Phase D §9.

The normative-force pass v1 articulates the semantic split as judgment-force-kind appearing at multiple sites of authorship-bearing form, with governance-force-kind always at the seam. The fork's resolution can be read as a mode-weighted question about which sites of judgment force become load-bearing in a given mode.

### Brand-System Layer Status (Named Structural Decision)

The brand-system layer's unmodeled status is reframed at this plateau from "limitation" to "named unresolved structural decision."

The brand platform deepening (Phase 2 third) pressured this from the abstraction angle: positioning, voice, values, audience — almost entirely prose-carried with structural void. The visual identity system deepening (Phase 2 fourth) pressured it from the concrete-constants angle: logo references, typography assignments, color codes, grid systems — brand-wide articulated constants with no first-class IA home. Two-angle pressure makes the gap harder to defer.

The decision the architecture must eventually take is *not* binary. Three shapes remain live:

- **First-class structural carriers throughout.** The IA gains structural homes for brand platform, brand-wide constants, photography conventions, and applied guidelines.
- **Terminal operator-mediated translation.** The IA explicitly accepts prose + operator judgment as the resolution path for upstream normative content; downstream packet / slot carriers absorb what intake produces.
- **Hybrid (most likely on current evidence).** Structural carriers for stable brand-wide constants (logo references; typography assignments; color codes; grid systems; demonstrated reference imagery — much of which the IA already partially carries via `reference_assets` and related lookup chains); operator-mediated judgment for abstraction, conflict, sparsity, override, and application.

The decision is not pre-resolved at this plateau. It is named.

The brand-system hybrid carrier decision surface v1 tests the three live shapes against the Phase 1 force-kind criterion set on the four landed Phase 2 deepenings plus the sparse-articulation fallback pressure conditions. No single shape carries the criterion set cleanly. If hybrid is the surviving shape, the surface implies three zones — structural carriers for stable brand-wide constants + approved references; operator-mediated translation for prose-rich directive content; an unresolved third zone of intake-time judgment trace currently uncovered.

The Phase 2 six-category cross-category synthesis (PR #244) sharpens the hybrid implication from three zones to **five or six zones**: (1) stable brand-wide constants + approved references; (2) prose / operator-mediated translation for abstraction and articulated conventions; (3) per-touchpoint and per-mode constraint structural representation (application-guidelines content; aspect-ratio-as-attribute and per-touchpoint approval-rule candidates paper-pressed); (4) governed-output continuity reference elevation beyond `reference_assets` (past-campaigns content pressuring the governed-asset layer); (5) temporal-state trace dimension (past-campaigns-specific current / historical / superseded classification); (6) intake-time judgment trace zone (carry-forward from the three-zone implication). No single shape carries all zones cleanly.

The brand-system carrier decision surface v2 (PR #248) substantively narrows this from a five-or-six-zone open question to a one-sub-zone open question. Of the six zones, **only Zones 1 and 2 are properly brand-system-layer carrier questions**. Zone 1 splits: **Zone 1A (approved reference imagery) is structurally earned** via the existing `reference_assets` + lookup chain + `asset_attachment` carriers (operationally tested across SKU + collection mode). **Zone 1B (brand-wide constants beyond approved references — color codes, typeface assignments, logo files as URLs, grid systems) is structurally suggested but held**; this is the residual brand-system-layer carrier question. **Zone 2 is prose / operator-mediated at current depth** as a deliberate architectural choice (not a deferred decision; encoding cross-brand vocabulary unification has not been earned, and operator continuity bridges the content). **Zones 3–6 are real held architectural questions but are not brand-system-layer carrier questions**: Zone 3 (per-touchpoint and per-mode constraint representation) belongs at packet/slot layer; Zone 4 (governed-output continuity reference elevation) belongs at governed-asset / reference-linkage layer; Zone 5 (temporal-state trace) belongs at reference-asset lifecycle layer; Zone 6 (intake-time judgment trace) belongs at intake / governance layer. Each is a candidate parallel decision surface or operational test target — not the brand-system structural decision. Hybrid (v1's Shape C) is therefore essentially already in operation at the brand-system layer; the structural decision has narrowed to whether to extend Zone 1's structural coverage to constants beyond approved references. Depth-authority: `docs/brand-system-carrier-decision-surface-v2.md`.

### Scale-of-Operation Held Question

At current evidence depth, prose discipline carries the intake-time judgment trace under one-operator continuous-context scale: operator continuity provides the bridge that structural carriers would otherwise have to provide. The brand intake judgment-trace dry run v1 surfaced this state honestly via a bounded sparse-articulation walk on one synthetic stack.

**Reframe (post-PR #239 + grounding-note v58):** Scale beyond operator continuity is an architectural premise of the project, not a strategic fork. The grounding note's stated purpose (scalable production pipelines for commercial visual assets; creative-operations / DAM / retail-ecommerce production / AI-workflow audiences) commits the architecture to that scope. The held question is therefore what trace and layer structure makes that generalization legible — not whether scale matters. Scale legibility is now a test criterion applied per layer and per discretion site (see the per-site trace-gap analysis in `docs/layered-reference-and-discretion-architecture-v1.md`), not a separate held fork. The gap between current evidence depth (single-operator continuous context) and target scope (operator-independent legibility) is named as scope of work.

Scale implication: if yes, the dry run surfaced three milestone-9 held candidates (`articulation_state`, `fallback_chain_position`, `derivation_basis`) plus a fourth surfaced dimension (presence-of-inspection trace) as conditionally load-bearing. None are operationally earned because the multi-operator / continuity-breaking scale condition was not tested directly.

The question is currently surfaced most sharply through intake-time judgment trace, but the scale issue is broader. The same operator-continuity-vs-structural-trace pattern may apply at curation seam (currently appears to hold structurally via the five-axis provenance + curator + `curation_pattern` carriers), at packet authorship (the upstream judgment-trace gap Phase D surfaced for PKT-CAMP-004 prompt-authoring and prompt-remediation moments), and at any judgment site where operator continuity has been the load-bearing carrier.

Posture: named, not resolved. Not committing to single-operator continuity as terminal scope. Not authorizing intake-trace carriers or any held candidate.

**Operational pressure now exists on this question** (post-PR #250). The Zone 6 continuity-breaking intake trace test v1 ([`docs/continuity-breaking-intake-trace-test-v1.md`](continuity-breaking-intake-trace-test-v1.md)) ran the simulated continuity-break pressure path against the brand intake judgment-trace dry run v1's substrate. Aggregate result: **trace partially breaks**. Most trace dimensions fail across most content pieces; small number of cases (enum-shaped content tied to mode-anchored vocabulary like `slot_role`; structurally-explicit linkage like `reference_assets` via `approved_reference_keys`) preserve partial success. The architecture relies on operator continuity for intake-reasoning preservation in nearly all free-form prose content. Held trace candidates (`articulation_state`, `derivation_basis`, `fallback_chain_position`, `contradiction_log` not-pressured) are now operationally-pressed, not merely paper-pressed. No carrier shape is earned. PR #241's operator-authored vs inferred-not-stated sub-distinction is sharpened to a candidate five-value `derivation_basis` (lifted-verbatim / inferred-from-evidence / worked-example-default / operator-authored / unresolved-marked). Negative-space trace surfaced as a sub-dimension of presence-of-inspection.

The test is a single-operator simulation against a synthetic stack — honest about both limits.

**The trace-carrier design decision surface has now landed** ([`docs/intake-trace-carrier-decision-surface-v1.md`](intake-trace-carrier-decision-surface-v1.md); PR #252). It converts PR #250's reconstruction-failure points into a carrier-shape comparison: six options pressured across nine criteria. Prose-only is marked insufficient for scale beyond operator continuity; inline annotations and per-clause structured tags are rejected (annotation brittleness without structural earn; operator burden disproportionate to coverage). Per-source-layer inspection log is structurally suggested for inspection-trace dimensions (presence-of-inspection; articulation-state; negative-space trace); intake decision record is structurally suggested for per-content derivation dimensions (derivation_basis with the five-value vocabulary; chain_position; source_layer; contradiction_resolution). **Option F — hybrid inspection log + intake decision record — is the surviving conceptual posture**, the only option addressing all six PR #250 trace dimensions. The decision surface explicitly does NOT promote Option F to schema; specific carrier-shape design (entity placement; field types; cross-entity relationships; rollup patterns) remains held. The architecture earns the conceptual carrier posture; the implementation shape is not earned.

A candidate next-pressure surface stands alongside the existing held inventory: a **real multi-operator continuity-break test** can now pressure a candidate Option F-shaped carrier set in mocked form (rather than pressuring the absence of carriers, which PR #250 already did and which would predictably reproduce the same break). Either path — that real test, or a structured IA v3 absorption move — would precede any specific carrier-shape design.

**The real multi-operator continuity-break test has now landed at one-reader mocked-shape depth** ([`docs/real-multi-operator-continuity-break-test-v1.md`](real-multi-operator-continuity-break-test-v1.md); PR #254). A fresh reader scored 10/10 against the dry-run-derived ground-truth key. **Aggregate result: trace holds at this test.** Every trace dimension Option F was designed to address scored SUCCESS (presence-of-inspection; articulation-state; chain-position; derivation-basis with five-value vocabulary; negative-space trace; both PR #241 + PR #250-sharpened sub-distinctions). The simulated finding from PR #250 (trace partially breaks against no carriers) shifts to trace holds against mocked Option F carriers — the test's intended pressure direction. **Option F is operationally supported at one-reader / one-substrate / mocked-shape depth.** The result does NOT earn specific carrier shape, schema, structured IA v3, validator, or orchestration. Contradiction-resolution remains non-pressured (substrate has no contradictions); multi-reader / cross-substrate / long-context / temporal-pressure generalization remains held. Two reader-flagged carrier-shape concerns surfaced as load-bearing for any specific implementation move: **granularity** (mocked content-piece-level granularity insufficient for prose-rich carriers with multi-clause derivation) and **reference opacity** (empty `asset_metadata` leaves reference-selection rationale unreconstructable for visual-reference governance use cases). Both extend the surface area of an eventual carrier-shape design without weakening Option F's conceptual posture.

**The Option F carrier-shape constraint surface has now landed** ([`docs/option-f-trace-carrier-shape-design-surface-v1.md`](option-f-trace-carrier-shape-design-surface-v1.md); PR #256). It converts PR #254's empirical support into bounded conceptual carrier-shape constraints, without authoring schema or implementation. The constraint vocabulary is labeled per five categories: **required by PR #254** (per-source-layer inspection log; intake decision record; five-value `derivation_basis` preserved without collapse; shared intake-event identifier; negative-space trace via inspection-log value space); **structurally suggested** (bidirectional inspection-log ↔ decision-record linkage; content-piece-level granularity as floor with mixed-granularity pattern; reference-selection rationale carrier among three structural alternatives — `asset_metadata` extension, separate reference-selection-rationale record, or intake-decision-record attachment; rejected-alternatives carrier as complement); **held pending contradiction-present substrate** (contradiction-resolution carrier shape; vocabulary; relationship to conflict-source records); **held pending cross-substrate pressure** (clause-level granularity for prose-rich content; multi-source divergence behavior; long-context durability; value-space extension for articulation-state and derivation-basis); **rejected / overbuilt** (prose-only reference rationale; event-level granularity; field-level granularity; collapsing the five-value `derivation_basis`). The surface does NOT author schema, entity placement, field types, cross-entity relationships, rollup patterns, structured IA v3, validator, or orchestration; PR #254's granularity and reference-opacity concerns are absorbed into the constraint vocabulary as held design questions, not closed. Zone 6 vs Zone 4 boundary is preserved: reuse-time reference governance routes to a held Zone 4 decision surface rather than collapsing into Zone 6 carrier-shape design.

**The contradiction-present continuity-break test has now landed** ([`docs/option-f-contradiction-present-continuity-break-test-v1.md`](option-f-contradiction-present-continuity-break-test-v1.md); PR #258). It addresses the contradiction-resolution dimension PR #256 held pending a contradiction-present substrate. Substrate: the dry-run kitchen-textiles synthetic stack plus a single minimal contradiction overlay — photography_style_guide articulates voice/tone register that contradicts asset_library evidence; operator resolves via the architecturally-grounded visual-as-fidelity-strategy / prose-as-fallback rule; asset_library carries. Mocked carrier shape: one of PR #256's three structurally-suggested alternatives — sub-field on intake decision record (D2) with five structured sub-fields (conflict_source; conflict_source_content; carried_source; resolution_rule; architectural_basis). **Aggregate result: trace holds at this test (13/13 fresh-reader reconstruction success).** **Contradiction-resolution sub-aggregate: 4/4** — all eight sub-dimensions (presence-of-contradiction; conflict-source identification; rejected-source content; carried-source content; carried-source identification; resolution rule; architectural basis; cross-cutting summary) reconstructed. The new `articulation_state: contradictory` value is operationally supported as architecturally distinct from `absent`. Base trace dimensions held under contradiction-overlay pressure (no regression from PR #254 baseline). **Option F's contradiction-resolution conceptual posture is operationally supported at one-reader / one-substrate / one-contradiction / clean-resolution-rule / mocked-shape depth.** The result does NOT earn the specific carrier shape: the mocked sub-field-on-decision-record shape worked but the test does not differentiate it from the other two PR #256-named structurally-suggested alternatives (separate `contradiction_log` entity; annotation on inspection log entry). Multi-reader / multi-contradiction / ambiguous-resolution-rule / cross-substrate / long-context generalization remains held. Clause-level granularity remains held per the reader-flagged weakness consistent across both PR #254 and PR #258.

**The multi-contradiction continuity-break test has now landed** ([`docs/option-f-multi-contradiction-continuity-break-test-v1.md`](option-f-multi-contradiction-continuity-break-test-v1.md); PR #260). It extends PR #258's single-contradiction baseline to a two-event substrate that pressures the carrier-shape choice PR #256 held among three structurally-suggested alternatives. Substrate: the dry-run kitchen-textiles synthetic stack plus a minimal multi-contradiction overlay — Event 1 carries forward PR #258's photography_style_guide tone vs asset_library evidence contradiction (1:1 structure); Event 2 introduces brand_platform internal-inconsistency (audience field carried; positioning field rejected) with multi-source convergent carriage [brand_platform.audience + asset_library] and 1:many decision impact (D1 + D2) under partial-architectural-grounding rule. Mocked carrier shape: extension of the sub-field-on-intake-decision-record alternative — list-valued contradiction_resolution, `affected_decisions` cross-reference, multi-source `carried_source` list, new `articulation_state` value `partial-with-internal-conflict`. **Aggregate result: trace holds at this test (14/14 fresh-reader reconstruction success).** **Multi-contradiction sub-aggregate: 5/5** — all eight multi-contradiction sub-dimensions reconstructed including Event 2's discriminating sub-dimensions (multi-source carriage; partial-architectural-grounding; 1:many decision impact) and the cross-event structural patterns (multi-event-per-decision; 1:many event-to-decisions; duplication of Event 2 across D1/D2). The new `articulation_state: partial-with-internal-conflict` value is operationally supported as architecturally distinct from `contradictory`, `partial`, and `absent`. Multi-source `carried_source` list representation and `affected_decisions` cross-reference are operationally supported. Base trace dimensions held under multi-contradiction-overlay pressure (no regression from PR #254 + PR #258 baselines). **Option F's multi-contradiction conceptual posture is operationally supported at one-reader / one-substrate / two-contradiction / mixed-1:1-and-1:many / clean-and-partial-architectural-grounding / mocked-shape depth.** **The carrier-shape choice is sharpened, not closed:** the extended sub-field-on-intake-decision-record shape works operationally, AND the reader explicitly identified Event 2's duplication across D1.contradiction_resolution and D2.contradiction_resolution as a structural pressure point. This strengthens the case that a separate `contradiction_log` entity (PR #256's second alternative) remains materially plausible because it would naturally dedupe what the sub-field shape duplicates, and that annotation-on-inspection-log (PR #256's third alternative) remains materially plausible for source-internal-inconsistency cases. Multi-reader / three-or-more-contradiction / ambiguous-resolution-rule / cross-substrate / long-context generalization remains held. Clause-level granularity remains held per reader-flagged weakness consistent across PR #254 + PR #258 + PR #260.

Candidate next-pressure surfaces stand alongside the existing held inventory: **ambiguous-resolution-rule substrate test** (would pressure the resolution_rule sub-field value-vocabulary question where no directly-architecturally-grounded rule applies and operator-judgment carries the resolution); **multi-reader replication of the Option F trace tests** (would pressure reader-variability across PR #254's no-contradiction baseline, PR #258's contradiction-present baseline, and PR #260's multi-contradiction baseline); **cross-substrate / prose-rich granularity test** (would pressure the clause-level vs mixed granularity decision PR #256 holds pending); **long-context / temporal-pressure test** (cross-time-gap reconstructability); **specific F-shaped carrier implementation / field design / structured IA v3 absorption** (would author concrete carrier shapes against PR #256's constraint vocabulary now operationally supported through three substrates — no-contradiction PR #254, contradiction-present PR #258, multi-contradiction PR #260 — with the carrier-shape selection question among PR #256's three contradiction-resolution alternatives sharpened by PR #260's surfaced duplication signal). None authorized.

**The intake-reference-rationale carrier decision surface has now landed** ([`docs/intake-reference-rationale-carrier-decision-surface-v1.md`](intake-reference-rationale-carrier-decision-surface-v1.md); PR #263) as the first post-milestone-11 architectural move. The artifact shifts axis from the Zone 6 / Option F trace plateau into a distinct architectural seam: intake-time reference-rationale carriers. It addresses the gap that three independent reader-flag observations across PR #254 + PR #258 + PR #260 named — selected references are structurally carried via `reference_assets` + `approved_reference_keys` + `asset_attachment`, but the *reasons* for selection (per-reference contribution; evidence basis; rejected alternatives; reference function from PR #239's nine-fold taxonomy at per-instance scope) are not. Carrier-shape options labeled per the PR #256 design-surface pattern: **R-A prose-only rationale REJECTED** (the empirical failure mode three readers flagged); **R-B `asset_metadata` extension STRUCTURALLY SUGGESTED** for per-reference intrinsic rationale; **R-C separate reference-selection-rationale record STRUCTURALLY SUGGESTED** for event-level rationale with rejected alternatives naturally accommodated; **R-D attachment to intake decision record STRUCTURALLY SUGGESTED** for event-level rationale tied to decision context (parallel to PR #256's mocked sub-field shape); **R-E rejected-alternatives complement STRUCTURALLY SUGGESTED AS COMPLEMENT**; **hybrid posture (R-B + R-C OR R-D + R-E) STRUCTURALLY SUGGESTED as conceptual posture** with R-C vs R-D HELD pending operational pressure. **Required by reader-flag evidence:** carrier-attached not prose-only; per-reference contribution reconstructable; rejected alternatives reconstructable; reference function reconstructable; evidence basis reconstructable. **Held pending operational pressure:** R-C vs R-D selection; selection-rationale category vocabulary; articulation-state at per-reference level; confidence vocabulary; multi-reference event scaling. **Held pending cross-substrate pressure:** negative/avoid reference handling; reference-rationale durability across reuse contexts. **Zone 6 vs Zone 4 boundary preserved:** intake-time reference rationale is this surface's scope; reuse-time governed-output continuity / derivative governance is Zone 4 — held as adjacent decision surface, not collapsed into intake-time scope. The surface does NOT author schema, entity placement, field types, cross-entity relationships, rollup patterns, structured IA v3, validator, or orchestration.

**The intake-reference-rationale operational pressure test has now landed** ([`docs/intake-reference-rationale-operational-pressure-test-v1.md`](intake-reference-rationale-operational-pressure-test-v1.md); PR #266) as Stage C of the operational pressure test PR #263's Self-Superseding Clause named. The test mocks the R-D shape (sub-field on intake decision record D3) within an R-B + R-D + R-E hybrid posture and pressures fresh-reader reconstruction of selected references, per-reference contribution, set-level rationale, and rejected alternatives. **Aggregate result: trace holds at this test (11/11 fresh-reader reconstruction success). Reference-rationale sub-aggregate: 7/7. Rejected-alternatives sub-aggregate: 3/3.** All scored dimensions reconstructed without partial or failure marks, including the within-packet multi-layer attribution case (REF-KT-003: creative_intent + bounded_creative_discretion), the multi-function-but-single-layer case (REF-KT-004), and the two-rules-applied rejection discrimination (visual-as-fidelity-strategy applied to REJ-A + REJ-B; SKU-driven mode hero-pattern boundary applied to REJ-C). **The R-B + R-D + R-E mocked hybrid held at one-reader / one-substrate depth.** **R-D was sufficient at this substrate depth AND structurally near the edge** per the reader's Q-11 carrier-shape pressure observation: "the pressure signal is not information loss; it is object-boundary ambiguity." **R-C remains materially plausible under growth conditions** (more references; more rejection classes; conflicting evidence; partial selection rationales) per the reader's directional signal. **Q-11 is unscored diagnostic signal, not proof.** **The test does NOT close the R-C vs R-D choice in either direction** — only R-D was mocked; R-C judgment is reader extrapolation from R-D experience, not direct operational pressure. The test cannot support "R-D beat R-C" or "R-C is unnecessary" or "R-C is required under any condition." Multi-reader / cross-substrate / symmetric-R-C-mocked / growth-pressure / multi-event / combined-with-Option-F-carriers / ambiguous-rejection-rule / long-context generalization all remain held. No carrier shape / schema / structured IA v3 / validator / orchestration / implementation is earned. Zone 4 reuse-time governed-output continuity / derivative governance remains adjacent and held.

**The Zone 4 governed-output continuity / reuse-derivative governance decision surface has now landed** ([`docs/governed-output-continuity-reuse-derivative-governance-decision-surface-v1.md`](governed-output-continuity-reuse-derivative-governance-decision-surface-v1.md); PR #268) as the first focused architectural treatment of Zone 4 since PR #248 located it. The artifact pursues a deliberate axis switch (per the post-PR #267 external sequencing synthesis) from intake-reference-rationale to Zone 4 — the adjacent and held surface PR #248 named and PR #263 / PR #265 / PR #266 / PR #267 explicitly preserved as not-collapsed-into intake-time scope. **Architectural object distinction preserved:** intake-time reference rationale (PR #263 / PR #266 axis) is why references are selected at intake (the operator's act of selecting from a candidate pool); Zone 4 (this surface) is what happens when governed outputs later re-enter the system as references (the dual-nature transition; reuse events; derivative obligations; family-coherence). Carrier-shape options labeled per the PR #263 design-surface pattern: **Z4-A prose-only reuse notes REJECTED** (same failure mode reader-flagged across PR #254 / #258 / #260); **Z4-B governed-output metadata extension STRUCTURALLY SUGGESTED** for asset-intrinsic state (continuity-anchor flag; family memberships; supersession state; derivative-children list); **Z4-C separate reuse / derivative event record STRUCTURALLY SUGGESTED** for event-level reuse trace; **Z4-D reference-linkage annotation STRUCTURALLY SUGGESTED** for per-linkage reuse-time annotation; **Z4-E asset-family / lineage record STRUCTURALLY SUGGESTED** for family-coherence scope; **hybrid posture (Z4-B + Z4-C OR Z4-D + Z4-E + reuse-rejected complement) STRUCTURALLY SUGGESTED as conceptual posture** with Z4-C vs Z4-D HELD pending operational pressure (parallel to PR #263's R-C vs R-D stance and PR #256's three-alternatives stance for contradiction-resolution). **Required by architectural premise:** carrier-attached not prose-only; reuse-type distinction reconstructable; source-authority-type reconstructable; downstream inheritance limits reconstructable; family-coherence relationships reconstructable; supersession events traceable. **Held pending operational pressure:** Z4-C vs Z4-D selection; reuse-type vocabulary; authority-type vocabulary; inheritance-dimension vocabulary; per-event approval-recurrence vocabulary; family-rule vocabulary. **Held pending cross-substrate pressure:** multi-mode reuse patterns; cross-brand reuse; cross-time reuse; cross-category reuse. The surface does NOT author schema, entity placement, field types, cross-entity relationships, rollup patterns, structured IA v3, validator, or orchestration. Intake-time reference rationale (PR #263 axis) is named adjacent and not collapsed throughout.

### Inventory

- **Apex Named Limitations.** Three carried forward from `docs/architecture-apex-definition-layer-v1.md`: (#1) specific aesthetic-carrier schema for brand-system, mode / category, or product-class — sharpened by the brand platform and VIS deepenings, status now named as a structural decision (above); (#2) precedence rule for inheritance and override — implicit-additive Model A holds where tested but no formal precedence rule earned; (#3) cross-base / cross-category / cross-brand pressure plus marketing-mode and campaign-mode operational evidence at full-flow depth — partially earned at this plateau (campaign-mode full-flow at PKT-CAMP-004 bounded depth; D2C base + Warm Restraint as cross-base + cross-brand evidence at bounded depth); remaining gaps named (marketing-mode full-flow; cross-category; broader cross-brand).
- **Composite-anchor structure.** Operationally deflated for collection mode; held for marketing and campaign modes; the unified `composite_anchor` polymorphic-entity question is unearned across the three non-SKU modes.
- **Inferred-not-stated structural representation.** Currently a prose discipline; surfaced by the sparse-articulation pressure note as a paper-pressed structural-carrier candidate (`derivation_basis` field; `articulation_state` flag); not earned.
- **Fallback-chain-position structural representation.** Chain order encoded in the structured IA model YAML (`fallback_chain` key, carried forward from v1 into v2); chain semantics carried in prose; structural representation of operator-resolved chain position is paper-pressed but not earned.
- **Operator-judgment representation.** Procedural content; not data; not modeled in the structured form. Carried in prose throughout Phase 1 + Phase 2 work.
- **Validator integration.** The structured IA model v2's YAML appendix could be derived as Pydantic models for `src/asset_pipeline_ask/` consumption when an operational consumer surfaces the need; not earned by current pressure.
- **Phase 2 input-category deepening surface.** All six brand-system input categories now at deepening depth; the cross-category synthesis (PR #244) reads across the six; structured IA model v2 (PR #246) absorbs the stable enumeration into structured form; the brand-system carrier decision surface v2 (PR #248) narrows the brand-system structural decision to a residual Zone 1B question and reroutes Zones 3–6 to adjacent decision surfaces; the Zone 6 continuity-breaking intake trace test v1 (PR #250) operationally pressures Zone 6 at simulated continuity-break depth (aggregate result: trace partially breaks); the intake trace carrier decision surface v1 (PR #252) converts PR #250's reconstruction-failure points into a carrier-shape comparison and identifies Option F as the surviving conceptual posture; the real multi-operator continuity-break test v1 (PR #254) pressures mocked Option F carriers against a fresh reader and finds trace holds at one-reader / one-substrate / mocked-shape depth — without earning specific carrier shape; the Option F trace carrier shape design surface v1 (PR #256) converts that empirical support into bounded carrier-shape constraints (required / structurally suggested / held pending contradiction-present substrate / held pending cross-substrate pressure / rejected) without authoring schema, entity placement, or implementation; the Option F contradiction-present continuity-break test v1 (PR #258) addresses the contradiction-resolution dimension PR #256 held pending and finds trace holds (13/13) and contradiction-resolution sub-aggregate holds (4/4) at one-reader / one-substrate / one-contradiction / clean-resolution-rule / mocked-shape depth — without earning the specific contradiction-resolution carrier shape over the other two PR #256-named alternatives; the Option F multi-contradiction continuity-break test v1 (PR #260) extends PR #258 to a two-event substrate and finds trace holds (14/14) and multi-contradiction sub-aggregate holds (5/5) at one-reader / one-substrate / two-contradiction / mixed-1:1-and-1:many / partial-architectural-grounding / mocked-shape depth — sharpening but not closing the carrier-shape choice among PR #256's three alternatives (the duplication of Event 2 across D1/D2 was reconstructable and explicitly named by the reader as a structural pressure point, which strengthens the case that a separate `contradiction_log` entity remains materially plausible). Whether the architecture next moves toward a Zone 1B operational test, ambiguous-resolution-rule substrate test (for Option F contradiction-resolution rules), multi-reader replication of the Option F trace tests OR of the intake-reference-rationale operational pressure test, cross-substrate / prose-rich granularity test, three-or-more-contradiction substrate test, long-context / temporal-pressure test, specific F-shaped carrier implementation / field design / structured IA v3 absorption, symmetric R-C-mocked test against the intake-reference-rationale substrate (parallel to PR #266's R-D mock), growth-pressure reference-rationale substrate (more references; more rejection classes; conflicting evidence; partial selection rationales — the conditions PR #266's reader Q-11 named as natural-fit-for-R-C), cross-substrate reference-rationale test, combined Option-F-trace + intake-reference-rationale carrier interaction test (architectural seam coherence between the two carrier families), ambiguous / operator-judgment rejection-rule substrate test (parallel to ambiguous-resolution-rule for contradiction-resolution), Zone 4 operational pressure test against mocked reuse / derivative governance carriers (the natural successor PR #268's Self-Superseding Clause names; parallel to PR #266 for intake-reference-rationale), derivative-vs-reuse pressure substrate, superseded-asset reuse pressure substrate, family-coherence / lineage pressure substrate, multi-mode reuse substrate, cross-time reuse substrate, interaction test between intake-reference-rationale carriers and Zone 4 reuse-time carriers, an adjacent-layer decision surface (Zone 3 packet/slot constraints; Zone 5 reference-asset temporal-state trace), validator / orchestration spec, an architecture front-door wholesale rewrite, or a different next move is a strategic decision the synthesis, structured IA v2, carrier decision surface v2, Zone 6 test v1, trace-carrier decision surface v1, real-reader test v1, carrier-shape design surface v1, contradiction-present test v1, multi-contradiction test v1, milestone 11, intake-reference-rationale carrier decision surface v1, intake-reference-rationale operational pressure test v1, and Zone 4 governed-output continuity / reuse-derivative governance decision surface v1 explicitly do not authorize.
- **Phase 3 demonstration.** All-four-workflow Airtable demonstration after the IA model is understood; held per the redirection's three-phase plan.

These boundaries are load-bearing. Any reading of the current architecture that elides them mis-reads it.

## Depth Documents

Entry points for content depth. Full repo navigation lives in `docs/index.md`; this list is the focused entry-point set.

- [`docs/index.md`](index.md): full navigation across all repo artifacts
- [`docs/diagrams/asset-pipeline-ASK_architecture-tree.html`](diagrams/asset-pipeline-ASK_architecture-tree.html): visual architecture tree at orientation depth; regenerated when a plateau milestone marker lands; slower update cadence than the prose docs; the prose docs are authoritative if the two diverge
- [`docs/milestones/milestone-11-zone-6-option-f-carrier-shape-plateau.md`](milestones/milestone-11-zone-6-option-f-carrier-shape-plateau.md): current plateau marker (Zone 6 / Option F trace carrier shape plateau)
- [`docs/milestones/milestone-10-phase-2-six-category-synthesis-plateau.md`](milestones/milestone-10-phase-2-six-category-synthesis-plateau.md): prior plateau marker (Phase 2 six-category synthesis plateau)
- [`docs/milestones/milestone-9-curation-semantic-split-brand-system-decision-surface-plateau.md`](milestones/milestone-9-curation-semantic-split-brand-system-decision-surface-plateau.md): milestone-9 plateau marker
- [`docs/milestones/milestone-8-phase-2-structured-model-plateau.md`](milestones/milestone-8-phase-2-structured-model-plateau.md): milestone-8 plateau marker
- [`docs/structured-ia-model-v2.md`](structured-ia-model-v2.md): current canonical structured enumeration of the IA layered-content model (hybrid prose + YAML appendix; 17 top-level keys absorbing milestone-10 stable vocabulary)
- [`docs/structured-ia-model-v1.md`](structured-ia-model-v1.md): predecessor structured form (v1's nine top-level YAML keys preserved at v2 with `deepened_input_categories` and `held_questions` updated); authoritative for v1 content depth
- [`docs/architecture-apex-definition-layer-v1.md`](architecture-apex-definition-layer-v1.md): v1 apex definition-layer artifact; three Named Limitations
- [`docs/campaign-mode-fork-resolver-test-phase-d-findings-v1.md`](campaign-mode-fork-resolver-test-phase-d-findings-v1.md): Phase D bounded `refines` reading; depth-authority for the curation-premise fork's current status and the curation semantic split
- [`docs/normative-force-and-durable-layer-pressure-v1.md`](normative-force-and-durable-layer-pressure-v1.md): normative-force pass v1; depth-authority for the six-kind force-kind criterion set
- [`docs/brand-system-carrier-decision-surface-v2.md`](brand-system-carrier-decision-surface-v2.md): current brand-system carrier decision surface; depth-authority for the zone-by-zone narrowing (hybrid operative for Zones 1+2; Zone 1A structurally earned; Zone 1B residual held; Zone 2 deliberate prose; Zones 3–6 rerouted to adjacent layers)
- [`docs/brand-system-hybrid-carrier-decision-surface-v1.md`](brand-system-hybrid-carrier-decision-surface-v1.md): predecessor brand-system carrier decision surface; depth-authority for the original three-shape comparison and three-zone implication; preserved as historical record
- [`docs/brand-intake-judgment-trace-dry-run-v1.md`](brand-intake-judgment-trace-dry-run-v1.md): brand intake judgment-trace dry run v1; depth-authority for the sparse-articulation walk + Outcome read + Scale-of-Operation Held Question pressure
- [`docs/continuity-breaking-intake-trace-test-v1.md`](continuity-breaking-intake-trace-test-v1.md): Zone 6 continuity-breaking intake trace test v1 (first operational pressure surface post-milestone-10); depth-authority for the simulated continuity-break finding (trace partially breaks); sharpens held trace candidates from paper-pressed to operationally-pressed; surfaces five-value `derivation_basis` candidate and negative-space trace sub-dimension; no carrier shape earned
- [`docs/intake-trace-carrier-decision-surface-v1.md`](intake-trace-carrier-decision-surface-v1.md): intake trace carrier decision surface v1; depth-authority for the Zone 6 carrier-shape comparison (six options × nine criteria) and the Option F (hybrid inspection log + intake decision record) surviving conceptual posture; minimum conceptual trace vocabulary named without schema; explicit non-promotion of any carrier shape to schema; temporal_state intentionally Zone 5 (adjacent decision surface), not Zone 6
- [`docs/real-multi-operator-continuity-break-test-v1.md`](real-multi-operator-continuity-break-test-v1.md): real multi-operator continuity-break test v1 findings (Stage C of Zone 6 test sequence); depth-authority for the real-reader operational finding (trace holds at this test, 10/10 SUCCESS); Option F operationally supported at one-reader / one-substrate / mocked-shape depth; granularity + reference-opacity surfaced as shape-design concerns distinct from Option-F-conceptual-level; no carrier shape / schema / structured IA v3 / validator / orchestration earned
- [`docs/option-f-trace-carrier-shape-design-surface-v1.md`](option-f-trace-carrier-shape-design-surface-v1.md): Option F trace carrier shape design surface v1; depth-authority for the bounded carrier-shape constraint vocabulary derived from PR #254's empirical support (required by PR #254 / structurally suggested / held pending contradiction-present substrate / held pending cross-substrate pressure / rejected); explicit non-authoring of schema, entity placement, field types, cross-entity relationships, rollup patterns, structured IA v3, validator, or orchestration; Zone 6 vs Zone 4 boundary preserved (reuse-time reference governance routed to a held Zone 4 decision surface, not collapsed into Zone 6 carrier-shape design)
- [`docs/option-f-contradiction-present-continuity-break-test-v1.md`](option-f-contradiction-present-continuity-break-test-v1.md): Option F contradiction-present continuity-break test v1 findings; depth-authority for the contradiction-resolution dimension operational pressure PR #256 held pending (aggregate 13/13 trace holds; contradiction-resolution sub-aggregate 4/4); Option F's contradiction-resolution conceptual posture operationally supported at one-reader / one-substrate / one-contradiction / clean-resolution-rule / mocked-shape depth; `articulation_state: contradictory` operationally supported as architecturally distinct from `absent`; specific contradiction-resolution carrier shape NOT earned (the mocked sub-field-on-decision-record shape worked but test does not differentiate it from the other two PR #256-named alternatives); no schema / structured IA v3 / validator / orchestration earned
- [`docs/option-f-multi-contradiction-continuity-break-test-v1.md`](option-f-multi-contradiction-continuity-break-test-v1.md): Option F multi-contradiction continuity-break test v1 findings; depth-authority for the multi-contradiction conceptual posture and the carrier-shape sharpening signal (aggregate 14/14 trace holds; multi-contradiction sub-aggregate 5/5); Option F's multi-contradiction conceptual posture operationally supported at one-reader / one-substrate / two-contradiction / mixed-1:1-and-1:many / partial-architectural-grounding / mocked-shape depth; `articulation_state: partial-with-internal-conflict` operationally supported as architecturally distinct from `contradictory`, `partial`, and `absent`; multi-source `carried_source` list, `affected_decisions` cross-reference, and partial-architectural-grounding distinction all reconstructable; specific contradiction-resolution carrier shape NOT earned — test sharpens but does not close PR #256's three-alternative choice (sub-field-on-decision-record strengthened in narrow operational support; separate `contradiction_log` remains materially plausible because reader explicitly identified Event 2 duplication across D1/D2; annotation-on-inspection-log remains materially plausible for source-internal-inconsistency); no schema / structured IA v3 / validator / orchestration earned
- [`docs/intake-reference-rationale-carrier-decision-surface-v1.md`](intake-reference-rationale-carrier-decision-surface-v1.md): intake-reference-rationale carrier decision surface v1; depth-authority for the bounded carrier-shape constraint vocabulary for intake-time reference-rationale (first post-milestone-11 architectural move; design-surface successor to PR #256's pattern but for a distinct architectural seam); R-A prose-only rationale REJECTED; R-B `asset_metadata` extension + R-C separate reference-selection-rationale record + R-D intake-decision-record attachment STRUCTURALLY SUGGESTED in distinct roles; R-E rejected-alternatives complement STRUCTURALLY SUGGESTED AS COMPLEMENT; hybrid posture (R-B + R-C OR R-D + R-E) STRUCTURALLY SUGGESTED as conceptual posture with R-C vs R-D HELD pending operational pressure (analogous to PR #256's stance for contradiction-resolution); explicit non-authoring of schema, entity placement, field types, cross-entity relationships, rollup patterns, structured IA v3, validator, or orchestration; Zone 6 vs Zone 4 boundary preserved (reuse-time governed-output continuity / derivative governance is Zone 4 — held as adjacent decision surface, not collapsed into intake-time rationale scope)
- [`docs/intake-reference-rationale-operational-pressure-test-v1.md`](intake-reference-rationale-operational-pressure-test-v1.md): intake-reference-rationale operational pressure test v1 findings; depth-authority for the operational pressure on PR #263's R-B + R-D + R-E hybrid posture (aggregate 11/11 trace holds; reference-rationale sub-aggregate 7/7; rejected-alternatives sub-aggregate 3/3 at one-reader / one-substrate depth); R-D sufficient at this substrate depth AND structurally near the edge per reader's Q-11 carrier-shape pressure observation ("object-boundary ambiguity rather than information loss"); R-C remains materially plausible under growth conditions per reader directional signal; Q-11 unscored diagnostic signal — test does NOT close R-C vs R-D choice in either direction (only R-D was mocked; R-C judgment is reader extrapolation, not direct operational pressure); explicit non-earning of specific carrier shape / schema / structured IA v3 / validator / orchestration; multi-reader / cross-substrate / symmetric-R-C-mocked / growth-pressure / multi-event / combined-with-Option-F-carriers / ambiguous-rejection-rule / long-context generalization unearned; Zone 4 reuse-time governed-output continuity / derivative governance remains adjacent and held
- [`docs/governed-output-continuity-reuse-derivative-governance-decision-surface-v1.md`](governed-output-continuity-reuse-derivative-governance-decision-surface-v1.md): Zone 4 governed-output continuity / reuse-derivative governance decision surface v1; depth-authority for the bounded carrier-shape constraint vocabulary for reuse-time reference governance (first focused architectural treatment of Zone 4 since PR #248 located it; parallels PR #263's design-surface pattern at a different architectural axis); Z4-A prose-only reuse notes REJECTED; Z4-B governed-output metadata extension + Z4-C separate reuse / derivative event record + Z4-D reference-linkage annotation STRUCTURALLY SUGGESTED in distinct roles; Z4-E asset-family / lineage record STRUCTURALLY SUGGESTED for family-coherence scope; hybrid posture (Z4-B + Z4-C OR Z4-D + Z4-E + reuse-rejected complement) STRUCTURALLY SUGGESTED as conceptual posture with Z4-C vs Z4-D HELD pending operational pressure (parallel to PR #263's R-C vs R-D stance); explicit non-authoring of schema, entity placement, field types, cross-entity relationships, rollup patterns, structured IA v3, validator, or orchestration; intake-time reference rationale (PR #263 axis) preserved as adjacent and not collapsed throughout — the two surfaces address structurally different architectural objects
- [`docs/layered-reference-and-discretion-architecture-v1.md`](layered-reference-and-discretion-architecture-v1.md): layered reference and discretion architecture v1; depth-authority for the nine-fold reference-function taxonomy, the normative-hierarchy framing across the eight IA layers, the nine-site creative-discretion decomposition with per-site trace-gap analysis, and the dining-vs-bedroom cross-category conceptual pressure
- [`docs/brand-discovery-digestion-layered-intake-architecture-v1.md`](brand-discovery-digestion-layered-intake-architecture-v1.md): layered intake architecture v1; depth-authority for the seven-stage extraction sequence, the operator-authored vs inferred-not-stated distinction, and the three-zone hybrid implication (subsequently sharpened to five-or-six-zone implication in PR #244)
- [`docs/brand-system-input-cross-category-synthesis-v1.md`](brand-system-input-cross-category-synthesis-v1.md): six-category cross-category synthesis v1; depth-authority for the six-category comparison matrix, the nine-relation cross-category vocabulary, the extraction-sequence refinement (default graph with conditional jump-points), and the five-or-six-zone hybrid implication
- [`docs/brand-system-input-visual-identity-system-to-ia-mapping-v1.md`](brand-system-input-visual-identity-system-to-ia-mapping-v1.md): VIS deepening; depth-authority for the brand-wide-constants angle of the brand-system layer structural decision
- [`docs/brand-system-input-brand-platform-to-ia-mapping-v1.md`](brand-system-input-brand-platform-to-ia-mapping-v1.md): brand platform deepening; depth-authority for the abstraction angle of the brand-system layer structural decision
- [`docs/brand-system-input-application-guidelines-to-ia-mapping-v1.md`](brand-system-input-application-guidelines-to-ia-mapping-v1.md): application guidelines deepening; depth-authority for application guidelines as the operationalization layer of the brand-system stack
- [`docs/brand-system-input-past-campaigns-to-ia-mapping-v1.md`](brand-system-input-past-campaigns-to-ia-mapping-v1.md): past campaigns deepening; depth-authority for past campaigns as historical execution evidence and the temporal-state trace candidate dimension
- [`docs/ia-layered-content-redirection-note-v1.md`](ia-layered-content-redirection-note-v1.md): the IA layered-content redirection that opened the trajectory milestones 8 and 9 mark
- [`docs/ia-layered-content-cross-mode-synthesis-v1.md`](ia-layered-content-cross-mode-synthesis-v1.md): Phase 1 cross-mode synthesis; carrier-status matrix; cross-mode layer map
- [`docs/brand-discovery-digestion-architecture-sketch-v1.md`](brand-discovery-digestion-architecture-sketch-v1.md): Phase 2 opening sketch; brand-system input categories; intake-to-IA mapping
- [`docs/brand-discovery-sparse-articulation-fallback-pressure-v1.md`](brand-discovery-sparse-articulation-fallback-pressure-v1.md): sparse-articulation fallback pressure; six-layer fallback chain
- [`docs/collection-mode-operational-test-findings-v1.md`](collection-mode-operational-test-findings-v1.md): collection-mode operational deflation evidence
- [`docs/milestones/milestone-7-v1-sweep-closure-plateau.md`](milestones/milestone-7-v1-sweep-closure-plateau.md): the milestone-7 plateau marker; v1 sweep closure
- [`AGENTS.md`](../AGENTS.md): single-node operating model + agent-agnostic workflow rules

The four Phase 1 mode sketches (B1–B4), the four Phase 2 input-category deepenings (photography / image style guide; asset library; brand platform; visual identity system), the structured IA model options note, the cross-mode probe series, the four-mode truth-and-constraint pressure note, the campaign-mode fork-resolver Phase A–D arc, and the full operational findings chain all live in `docs/index.md` for navigation.

This file is current as of milestone 11 (the Zone 6 / Option F trace carrier shape plateau), alongside milestone 10 (the Phase 2 six-category brand-system input synthesis plateau) which it does not supersede. The current state includes structured IA model v2 (PR #246) as the canonical structured-form companion, brand-system carrier decision surface v2 (PR #248) narrowing the brand-system structural decision to a residual Zone 1B question, the Zone 6 continuity-breaking intake trace test v1 (PR #250) as the first operational pressure surface post-milestone-10 (aggregate result: trace partially breaks against no carriers), the intake trace carrier decision surface v1 (PR #252) identifying Option F (hybrid inspection log + intake decision record) as the surviving conceptual carrier posture without promoting any carrier shape to schema, the real multi-operator continuity-break test v1 (PR #254) pressuring mocked Option F carriers against a fresh reader with aggregate result trace holds at one-reader / one-substrate / mocked-shape depth, the Option F trace carrier shape design surface v1 (PR #256) converting that empirical support into a bounded carrier-shape constraint vocabulary (required / structurally suggested / held pending contradiction-present substrate / held pending cross-substrate pressure / rejected) without authoring schema or implementation, the Option F contradiction-present continuity-break test v1 (PR #258) addressing the contradiction-resolution dimension PR #256 held pending with aggregate result trace holds (13/13) and contradiction-resolution sub-aggregate holds (4/4) at one-reader / one-substrate / one-contradiction / clean-resolution-rule / mocked-shape depth — without earning the specific contradiction-resolution carrier shape over the other two PR #256-named alternatives, the Option F multi-contradiction continuity-break test v1 (PR #260) extending PR #258 to a two-event substrate with aggregate result trace holds (14/14) and multi-contradiction sub-aggregate holds (5/5) at one-reader / one-substrate / two-contradiction / mixed-1:1-and-1:many / partial-architectural-grounding / mocked-shape depth — sharpening but not closing PR #256's three-alternative carrier-shape choice, and milestone 11 marking the saturation of the Zone 6 / Option F arc as a coherent foundation that subsequent pressure can build on rather than continuing to extend. It absorbs the post-milestone-9 normative-force pass v1, the brand-system hybrid carrier decision surface v1 (preserved as predecessor), the brand intake judgment-trace dry run v1, the layered reference and discretion architecture v1 (PR #239), the layered intake architecture v1 (PR #241), the application guidelines deepening (PR #242), the past campaigns deepening (PR #243), the cross-category synthesis v1 (PR #244), the milestone-10 plateau marker (PR #245), the structured IA model v2 (PR #246), the brand-system carrier decision surface v2 (PR #248), the Zone 6 continuity-breaking intake trace test v1 (PR #250), the intake trace carrier decision surface v1 (PR #252), the real multi-operator continuity-break test v1 (PR #254), the Option F trace carrier shape design surface v1 (PR #256), the Option F contradiction-present continuity-break test v1 (PR #258), the Option F multi-contradiction continuity-break test v1 (PR #260), the milestone-11 plateau marker, the intake-reference-rationale carrier decision surface v1 (PR #263), the intake-reference-rationale operational pressure test v1 (PR #266), and the Zone 4 governed-output continuity / reuse-derivative governance decision surface v1 (PR #268), plus the Scale-of-Operation reframe and the conceptual claims newly earned at PR #239 and at the Phase 2 six-category synthesis plateau. It will need to be revised when the next architectural plateau is reached, when Phase 3 produces operational evidence, when one of the held questions earns resolution beyond bounded scope, when one of the candidate next-pressure surfaces (Zone 1B operational test; ambiguous-resolution-rule substrate test for Option F contradiction-resolution rules; multi-reader replication of the Option F trace tests OR of the intake-reference-rationale operational pressure test; three-or-more-contradiction substrate test; cross-substrate / prose-rich granularity test; long-context / temporal-pressure test; specific F-shaped carrier implementation / field design / structured IA v3 absorption; symmetric R-C-mocked test against the intake-reference-rationale substrate; growth-pressure reference-rationale substrate; cross-substrate reference-rationale test; combined Option-F-trace + intake-reference-rationale carrier interaction test; ambiguous / operator-judgment rejection-rule substrate test; Zone 4 operational pressure test against mocked reuse / derivative governance carriers; derivative-vs-reuse pressure substrate; superseded-asset reuse pressure substrate; family-coherence / lineage pressure substrate; multi-mode reuse substrate; cross-time reuse substrate; interaction test between intake-reference-rationale carriers and Zone 4 reuse-time carriers; Zone 3 application-guidelines cross-touchpoint test at packet/slot layer; Zone 5 past-campaigns temporal-state operational test at reference-asset lifecycle layer; validator / orchestration spec; architecture front-door wholesale rewrite; milestone marker for a future plateau) is authorized and lands, or when a subsequent milestone marks a new plateau.
