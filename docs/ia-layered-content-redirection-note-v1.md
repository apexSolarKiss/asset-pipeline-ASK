# IA Layered-Content Redirection Note // v1

This note marks the project's transition from further Airtable prototype mutation to on-paper IA layered-content articulation as the active frontier. It exists so that the redirection is visible inside the repo, not only in external context.

## Posture

```text
directional hold / transition marker
not a workflow rule
not a milestone
not a planning chain
not the current architecture synthesis
```

## Triggering Clarification

The triggering source-of-intent is `asset-pipeline-ASK_redirection-to-IA-content-work_v1.md` in the project's external sources-of-intent directory. The durable layer of that clarification has been absorbed into grounding-note v51.

This repo-local note carries the temporal-sequencing piece of the same clarification, scoped to what a returning repo reader needs to see.

## What This Note Records

The project's center is the **layered IA content model** itself — the structured articulation of what kinds of information, constraints, and references operate at brand layer, category layer, collection layer, packet layer, and slot layer; what is shared upstream and what is specific downstream; how the layers inherit, override, and combine across the four worked-example workflow modes (SKU-driven, collection / merchandising, marketing / message-driven, brand campaign / editorial).

Airtable remains a pressure surface and a later demonstration surface. It is not the project center.

The clarification has two layers:

- **Durable clarification.** The project's center has always been the layered IA content model; the prototype is a pressure surface for that model. Absorbed into grounding-note v51.
- **Temporal sequencing.** Pause further Airtable prototype mutation. Do on-paper IA layered-content work next. Return to Airtable later as a demonstration of the IA, not as a probe of foundational premises.

## Phases

Named at one-line depth only. Phase detail lives in the source-of-intent file; this note does not expand it.

- **Phase 1.** On-paper IA layered-content work across the four worked-example workflow modes.
- **Phase 2.** Brand discovery / digestion architecture — the structured intake from a real client brand system into the layered IA.
- **Phase 3.** All-four-workflow Airtable demonstration after the IA model is understood.

Campaign / editorial mode is also the strongest pressure point for whether "curation as the singular runtime creative-discretionary act" remains sufficient, because campaign-concept selection may prove to be upstream setup, a different runtime creative-discretionary act, or a premise that needs refinement.

## Brand Discovery / Digestion as Consulting-Facing Surface

Brand discovery / digestion is also the strongest consulting-facing surface of the architecture: it translates a client's existing brand system into the layered IA before any tool implementation begins.

## What Is Paused

- `Campaign-Driven D2C Home-Goods v1` base setup
- Any other Airtable prototype mutation work
- Further accrual of `connector-limitations` doc entries
- The per-phase plan / structured-change-summary cadence in the existing campaign-mode chain

Already-landed work stays in the repo. Nothing is reverted.

## What Is Not Paused

- Repo navigation / docs hygiene at the smallest honest unit
- Architectural-question hold artifacts that frame Phase 1 or Phase 2 work
- External writing

## Self-Superseding Clause

This note is a transition marker. It should be superseded once Phase 1 (and possibly Phase 2) produces enough material for a milestone-8 synthesis to absorb the redirection alongside v1 sweep closure, the collection-mode operational deflation of paper strain, and the cross-mode IA content model.

The eventual rewrite of `docs/architecture.md` to absorb the apex definition-layer artifact and the Phase 1 sketches as a single consolidated current-architecture document is a separate downstream move; this note does not anticipate its content.

A later synthesis may also need to make the project's architecture-development method legible as a first-class repo subject; that is outside this transition note.
